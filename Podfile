# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Firebase-singup-login-swift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Firebase-singup-login-swift

  target 'Firebase-singup-login-swiftTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Firebase-singup-login-swiftUITests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'

end
